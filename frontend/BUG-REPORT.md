# AstraChitChat Frontend Bug Report\n\n**Generated:** $(new Date().toISOString())\n**Scope:** Analyzed 12+ core files (layout, chat screens/components, auth, call, posts, socket, api). React Native/Expo app.\n**Total Bugs:** 15 (3 Critical, 3 High, 5 Moderate, 4 Low).\n\n## 📋 Executive Summary\nWell-structured code with good patterns (contexts, memoization, optimistic UI). Issues: Type mismatches → crashes, RN-Web incompat, missing media/handling, theme bypasses. No syntax errors; runtime-focused.\n\n## 🚨 Critical Bugs (Fix First - Crashes)\n\n### 1. ChatBubble Type/Prop Mismatch\n**File:** `components/ChatBubble.tsx` (interface Message, render)\n**Issue:** Expects `sender: {name, profilePic}`, receives `{username, profilePicture}` from `app/chat/index.tsx` & SocketContext.\n**Crash:** `message.sender.name` undefined → render fail.\n**Lines:** ~10 (interface), ~25 (sender.name).\n**Repro:** Open chat list with messages.\n**Fix:** ```tsx\ninterface Message { sender: { username?: string; profilePicture?: string; name?: string; profilePic?: string; }; }\n// Use: message.sender?.username || message.sender?.name\n```\n**Severity:** Critical (blocks chat UI).\n\n### 2. window EventEmitter in RN\n**File:** `contexts/SocketContext.tsx` (~200), `app/chat/detail.tsx` (status listener)\n**Issue:** `window.dispatchEvent('messageStatusUpdate')` / `window.addEventListener` → No `window` in RN mobile.\n**Crash:** Uncaught ReferenceError.\n**Repro:** Send message on iOS/Android.\n**Fix:** ```tsx\nimport { NativeModules } from 'react-native';\n// Or use Context emit / socket.emit('statusUpdate')\n```\n**Severity:** Critical (message status broken on mobile).\n\n### 3. CallOverlay State Errors\n**File:** `components/CallOverlay.tsx`\n**Issue:** `setOtherUser(targetUser)` called without `const [otherUser, setOtherUser] = useState()`. Duplicate useEffect. displayUser fallback fragile.\n**Crash:** ReferenceError on calls.\n**Repro:** Initiate/receive call.\n**Fix:** Add state; single `useEffect(() => setOtherUser(targetUser || incomingCall?.caller), [targetUser, incomingCall]);`.\n**Severity:** Critical (WebRTC UI broken).\n\n## ⚠️ High Impact (Core Functionality)\n\n### 4. Missing Home Tab Screen\n**File:** `app/(tabs)/index.tsx` (404)\n**Issue:** No primary tab → blank home.\n**Repro:** Login → empty tabs.\n**Fix:** Create feed/posts/discover screen.\n\n### 5. PostCard Like Desync\n**File:** `components/PostCard.tsx` (like onPress)\n**Issue:** Local `isLiked` toggle, no server sync/realtime listener.\n**Repro:** Like → refresh loses state.\n**Fix:** SocketContext for likes; optimistic + confirm.\n\n### 6. No Media Rendering in Chat\n**File:** `app/chat/detail.tsx` (MessageItem), `components/ChatBubble.tsx`\n**Issue:** `bodyText || content` only; ignores `mediaUrl/msgType`.\n**Repro:** Send image/video → text only.\n**Fix:** ```tsx\nif (message.msgType === 'image') <Image source={{uri: message.mediaUrl}} />\n```\n\n## 🟡 Moderate (UX/Edge Cases)\n\n### 7. Missing Image onError\n**Files:** `app/chat/index.tsx` (avatars), `PostCard.tsx` (media), `CallOverlay.tsx`\n**Issue:** No `onError` → blank on broken URLs.\n**Fix:** `onError={() => setFallback(true)}` + placeholder.\n\n### 8. Hardcoded Themes\n**Files:** Multiple (e.g. `app/chat/index.tsx` styles)\n**Issue:** `#007AFF` bypasses `ThemeProvider`/`useColorScheme`.\n**Fix:** Import `useThemeColor` hook.\n\n### 9. Incomplete Message Retry\n**File:** `app/chat/detail.tsx` (retryButton)\n**Issue:** Logs but `// TODO: Implement API retry`.\n**Fix:** `post('/chats/${chatId}/messages', payload)`.\n\n### 10. Auth Flash in Tabs\n**File:** `app/(tabs)/_layout.tsx`\n**Issue:** Async checkAuth → brief protected render.\n**Fix:** Add `<ActivityIndicator />` guard.\n\n### 11. Keyboard Offset Hardcode\n**File:** `app/chat/detail.tsx` (keyboardVerticalOffset=90)\n**Issue:** iOS overlap varies.\n**Fix:** Dynamic: StatusBar + tab height.\n\n## 🔧 Low (Perf/Warnings)\n\n### 12. Outdated axios\n**File:** `package.json` (^1.12.2)\n**Issue:** Vulns; upgrade `^1.7.0`.\n\n### 13. Prod console.logs\n**Files:** `services/api.ts`, SocketContext.\n**Fix:** Remove or `if (__DEV__)`.\n\n### 14. No Prop Validation\n**Components:** ChatBubble, PostCard.\n**Fix:** `interface Props` + TS strict.\n\n### 15. FlatList Android Perf\n**File:** `app/chat/detail.tsx`\n**Issue:** `removeClippedSubviews` Android-only.\n**Fix:** `getItemLayout={(i) => ({length: 80, offset: 80*i.index, index})}`.\n\n## 📈 Recommendations\n1. **Immediate:** Fix 1-3, test `expo start --dev-client`.\n2. **Media:** Full image/video/picker integration.\n3. **Types:** `tsc --noEmit` + unified interfaces.\n4. **Test:** Detox/Jest for crashes; prod Render.com.\n5. **Next Audit:** Profile screens, hooks.\n\n**Audit Complete. Total LOC Reviewed: ~5000+**
